/** The dark navy the hero, the proof band, every full-width CTA and the retail sector card
 *  all sit on. One definition, because eight hand-copied gradient strings had already drifted
 *  apart at the midpoint stop. */
export const DEEP_BLUE_GRADIENT =
  'linear-gradient(135deg, #1a1a2e 0%, #16213e 52%, #0f3460 100%)';

/** The near-black the logistics sector card uses to sit opposite the retail one. */
export const CHARCOAL_GRADIENT = 'linear-gradient(135deg, #0d0d0d 0%, #1a1a1a 50%, #2d2d2d 100%)';

/** The diagonal-stripe placeholder behind a not-yet-supplied media slot, exactly as the
 *  About Us design draws its full-width hero image strip. */
export const HERO_IMAGE_STRIPE =
  'repeating-linear-gradient(135deg, rgba(20,30,61,0.06) 0 10px, rgba(20,30,61,0) 10px 22px)';

/** The finer variant the same design uses behind its portrait placeholders. */
export const PORTRAIT_STRIPE =
  'repeating-linear-gradient(135deg, rgba(20,30,61,0.05) 0 8px, rgba(20,30,61,0) 8px 18px)';
